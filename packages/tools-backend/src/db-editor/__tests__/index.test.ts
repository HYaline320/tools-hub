// packages/tools-backend/src/sql-editor/index.test.ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';



// 在测试前
beforeAll(async () => {
  //
});

afterAll(async () => {
  // 
});


describe('SqlEditorHandler - getDbInfo', () => {
  it.only('应该返回正确的表信息和字段', async () => {

    // const result = await (handler as any).getDbInfo(testDb, {
    //   id: 'test.db',
    //   originalName: 'test.db',
    //   mimeType: 'application/octet-stream',
    // });

    // expect(result.data).toBeDefined();
    // expect(result.data.type).toBe('sqlite');
    // expect(result.data.tables).toContain('users');
    // expect(result.data.tableDetails).toHaveProperty('users');
    // expect(result.data.tableDetails.users.columns).toEqual(['id', 'name', 'age']);
    // expect(result.data.tableDetails.users.rowCount).toBe(2);


  });
});
